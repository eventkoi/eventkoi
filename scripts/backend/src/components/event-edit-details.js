import { Box } from "@/components/box";
import { Heading } from "@/components/heading";

export function EventEditDetails({ loading, event }) {
  return (
    <Box>
      <Heading level={3}>Event details</Heading>
    </Box>
  );
}
